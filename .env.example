# Copy this file to `.env` and fill in your own API key.
# Only needed to regenerate solutions from scratch (Option B in the README);
# the committed generations let you run the whole analysis without any key.

OPENAI_API_KEY=
